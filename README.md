# Personal Website — Lewis Kimari

This is the source for Lewis Kimari's personal portfolio website.

## Deployment
This repository contains a static site. Deploy using your preferred static host (for example GitHub Pages, Render, or another provider). For local testing, run `npm start` to serve the site using `live-server`.

## Files added
- `.gitignore` — standard ignores
- `README.md` — this file

## Notes
- If you want me to push files and create the repo structure locally, tell me and I can create a local git repo and commit the changes for you. I cannot create the remote GitHub repo on your behalf without your credentials.